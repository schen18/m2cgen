# Refresh target matrix (audit 2026-09; see AUDIT_AND_UPGRADE_PLAN.md).
# Python >= 3.12 required by xgboost>=3.3; scikit-learn 1.9 needs >= 3.11.

# Other stuff
numpy==2.5.2
scipy==1.18.0

# Supported models
scikit-learn==1.9.0
xgboost==3.4.1
lightgbm==4.7.0
statsmodels==0.15.0
wittgenstein==0.3.5

# Testing tools
duckdb==1.5.5
flake8==7.3.0
isort==9.0.1
pytest==9.1.1
pytest-mock==3.15.1
pytest-cov==7.0.0

# NOTE: py-mini-racer (JS e2e executor of the original project) does not work on
# modern Python; the JavaScript e2e executor uses a Node.js subprocess instead.

# Publishing
build
twine
